# Voice Input GUI 配置文件
# 只保留 GUI 和 ASR 客户端必需的配置

# ASR 服务器配置
# 连接本地 FunASR WebSocket 服务器进行语音识别
[asr]
server_host = "127.0.0.1"
server_port = 10095
server_mode = "2pass"
chunk_size = "5,10,5"
chunk_interval = 10

# 窗口配置
[window]
